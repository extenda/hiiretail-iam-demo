export const getPermissions = async (): Promise<string[]> => {
  // const token = await firebase.app().auth().currentUser?.getIdToken();
  // const tenantId = localStorage.getItem('tenantId') as string;


//  Use IAM API (https://developer.hiiretail.com/api/iam-api#operation/getOwnPermissions) to fetch user's permissions here
  return [];
};
